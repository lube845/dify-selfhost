registry=https://registry.npmmirror.com/
# Skip lifecycle scripts (`prepare`, `postinstall`, ...). The production
# web image already ran them once when it was built; re-running on every
# container start requires git / python which aren't in the slim image
# and just slows startup.
ignore-scripts=true